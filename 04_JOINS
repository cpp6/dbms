CREATE DATABASE Customer_Order_DB;

USE Customer_Order_DB;

CREATE TABLE customer (
    customer_id INT PRIMARY KEY,
    customer_name VARCHAR(50),
    phone_no BIGINT, 
    email_id VARCHAR(50),
    address VARCHAR(100)
);

CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    customer_id INT,
    product VARCHAR(50),
    quantity INT,
    price INT,
    FOREIGN KEY (customer_id) REFERENCES customer(customer_id)
);

INSERT INTO customer VALUES
(1, 'Amit', 9876543210, 'amit@gmail.com', 'Pune'),
(2, 'Neha', 8765432109, 'neha@gmail.com', 'Mumbai'),
(3, 'Rahul', 7654321098, 'rahul@gmail.com', 'Delhi');

INSERT INTO orders VALUES
(101, 1, 'Laptop', 1, 50000),
(102, 2, 'Mobile', 2, 20000),
(103, 1, 'Keyboard', 1, 2000);

SELECT * FROM customer;
SELECT * FROM orders;

SELECT c.customer_id, c.customer_name, o.product, o.quantity, o.price
FROM customer c
INNER JOIN orders o ON c.customer_id = o.customer_id;

SELECT c.customer_id, c.customer_name, o.product, o.quantity, o.price
FROM customer c
LEFT JOIN orders o ON c.customer_id = o.customer_id;

SELECT o.customer_id, c.customer_name, o.product, o.quantity, o.price
FROM customer c
RIGHT JOIN orders o ON c.customer_id = o.customer_id;

SELECT c.customer_id, c.customer_name, o.product, o.quantity, o.price
FROM customer c
LEFT JOIN orders o ON c.customer_id = o.customer_id
UNION
SELECT o.customer_id, c.customer_name, o.product, o.quantity, o.price
FROM customer c
RIGHT JOIN orders o ON c.customer_id = o.customer_id;
